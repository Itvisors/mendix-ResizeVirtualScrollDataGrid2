import { border, brand, background, spacing } from "../../../../theme/native/custom-variables";

export const dashboardItem = {
    container: {
        aspectRatio: 1,
        paddingVertical: spacing.regular,
        alignItems: "center",
        justifyContent: "space-between"
    },
};
